{
  "name": "@tcsi/worker",
  "private": true,
  "version": "0.1.0",
  "description": "TCSI Accounting Railway worker",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.116.0",
    "@tcsi/contracts": "0.1.0",
    "zod": "4.6.5"
  }
}
